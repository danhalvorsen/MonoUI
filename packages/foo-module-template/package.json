{
  "name": "foo-module-template",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && vite build",
    "dev": "vite --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit --project tsconfig.json"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.0.0",
    "vite-plugin-dts": "^3.9.0",
    "rimraf": "^6.0.1"
  }
}