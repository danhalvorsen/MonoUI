import { IConfiguration } from "./IConfiguration.js";

/**
 * Represents the configuration for a visual object.
 * Extends the base {@link IConfiguration} interface to provide additional properties or methods
 * specific to visual object configurations.
 */
export interface IVisualObjectConfiguration extends IConfiguration {
}
