declare module 'mr-style' {
  export interface IColorTokens {
    primary: string;
    secondary: string;
    // Add other color tokens as needed
  }

  export interface ISizeTokens {
    small: string;
    medium: string;
    large: string;
    // Add other size tokens as needed
  }

  export interface ISpacingTokens {
    tiny: string;
    small: string;
    medium: string;
    large: string;
    // Add other spacing tokens as needed
  }

  // Add other token interfaces as needed
}
