import { IVisualObject } from "mr-abstract-components";
import { VisualRectangle } from "mr-web-components";

export class Rectangle {
    visualObjects: (IVisualObject | VisualRectangle)[] = [];
    
    constructor() {
        
    }
}